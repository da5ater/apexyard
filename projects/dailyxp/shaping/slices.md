---
shaping: true
---

# DailyXP Phase R — Slices (Shape A breadboard, vertical)

**Status:** APPROVED with integrity correction (Mohamed, 2026-08-24): V9 engine-bind slice removed —
it deferred integration and recreated the horizontal-layer problem in reverse. **V1 is the only
stub-driven exception; every later slice binds the real engine incrementally.**
**Rule:** every slice ≥V2 delivers UI + real domain behavior + real persistence + visible evidence (R2/R3);
model/persistence touched only where the slice requires it.

| # | Slice | Mechanism | Real-engine binding | Demo statement |
|---|-------|-----------|---------------------|----------------|
| V1 | Shell + PLAY lands | A1+A2+K2+K4+K5 | **None — sole fixture-stub exception** (real QML shell prototype; N3 focus chain verdict lands here) | "Install plugin → panel opens to minimal PLAY with stub rail, tabs switch and keep state, Tab key walks every control with visible focus" |
| V2 | First commitment works | R4 step 1 + R7 | Real planning path: commitment create → `PlanningModel`/journal append → envelope save → `planningProjection` feeds rail; close/reopen/reboot replays from `$XDG_STATE_HOME/dailyxp/` | "Fresh install → 'Set your first commitment' → name+duration → Save → today-rail shows it from the real journal; kill shell, reopen — still there" |
| V3 | Session responds | R4 steps 2–3, R4.1 | Real session path: `sessionCommand` IPC verbs → `SessionModel`/journal; restart-safe replay reconstructs active session from frozen timestamps | "Press START → timer runs inline in rail → Pause/Resume → Finish → XP chip appears; kill shell mid-session, reopen — session resumes correctly" |
| V4 | Progress explains itself | R4.2 | Real `ProgressionModel`: ledger rows are genuine awards w/ rule version; JOURNEY tiles/bar read `progressionProjection` | "Open JOURNEY → LV/rank/Momentum + segmented XP bar reflect V3's real earned XP; XP Ledger sheet lists those exact awards" |
| V5 | Routine seeds the day | R4 recurring | Real routine generation: `PlanningModel` Routine → dated Occurrences via journal events | "SETUP → new routine 'Ruby 2h daily' → back to PLAY: today's occurrence card present, generated by the real planner" |
| V6 | Habits + overdue gentleness | R4.3 + R4.5 | Real `HabitModel` completion + real carryover/overdue semantics drive the single-suggestion rule | "Tap habit chip → completes for real; force an overdue occurrence → landing shows ONE small suggestion (start-smaller/reschedule/skip), never a list" |
| V7 | World motif + Recovery guard | R5.1 + R4.4 | Real `StoryModel` (provinces/Momentum/Hollow-King cause lines) + `RecoveryModel` with private-flag enforced at every write site | "WORLD shows goal-as-province cards + Momentum banner; footer Recovery chip confirms first; check-in/relapse writes stay privacy-gated in the real journal" |
| V8 | Settings/export/bar truth | K3, hygiene | Real settings persistence + existing `EventModel` JSON export + bar widget rebinding to live projections | "SETUP edits persist across restart; Export JSON produces a valid file of real events; bar shows Lv / running elapsed from the live store" |
| — | *(optional)* Adapter cleanup | — | Non-user-facing `/task` only if fixture scaffolding (N10/N11) leaves residue after V8 | "No demo — cleanup task, filed only if necessary" |

## Notes

- V1 also carries the R8 verdict: if Loader-mounted screens break focus traversal, pivot to visibility-toggle stacking inside V1 (pre-agreed fallback).
- Integration risk now front-loaded by design: each V2+ PR proves its engine binding immediately, so a broken assumption surfaces in its own small PR, not in an end-phase big-bang.
- Fixture loader retires progressively: each V2+ slice replaces its stub source with the real projection it needs; nothing waits for a final swap.
